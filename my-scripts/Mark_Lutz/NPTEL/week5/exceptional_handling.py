#!/usr/bin/python3.4

'''
try:
    piece of code

except(NameError): #handling single error
    handle the error in the way u want

except(IndexError,AppendError): #handling multiple errors
    handle the error in the way u want

except:
    if none of the above errors encountered the this will handle any other error

else:
    If there was no error and 'try' code was successfullt executed

'''

